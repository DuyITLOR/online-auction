{
	"name": "online-auction",
	"private": true,  
	"scripts": {
		"dev": "concurrently -n \"WEB,SERVER\" -c \"cyan,green\" \"pnpm --filter=client run dev --host\" \"pnpm --filter=server run dev --host\"",
		"dev:client": "pnpm --filter=client run dev",
		"dev:server": "pnpm --filter=server run dev",
		"build": "pnpm -r run build",
		"lint": "pnpm -r run lint"
	  },
	"devDependencies": {
	  "concurrently": "^8.2.2"
	}
  }